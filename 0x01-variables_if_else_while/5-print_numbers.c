#include <stdio.h>
/**
 * main - prints the alphabet in lowercase, followed by a new line
 * Return: 0 always
 */
int main(void)
{
int n;
for (n = 0; n < 10; n++)
{
printf("%d\n", n);
}
return (0);
}
