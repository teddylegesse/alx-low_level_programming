#include <stdio.h>
/**
 *  main - prints the numbers  in base 16, followed by a new line
 *  Return: 0 always
 */
int main(void)
{
int ba;
char ch;
for (be = 48; be <= 57; be++)
putchar(be);
for (ch = 'a'; ch <= 'f'; ch++)
putchar(ch);
putchar(10);
return (0);
}
