#include <stdio.h>
/**
 * main - prints the string type of intiger, followed by a new line
 * Return: 0 always
 */
int main(void)
{
int st;
for (st = 48; st <= 57; st++)
putchar(st);
putchar(10);
return (0);
}
