#include "main.h"
/**
 * print_square - function that prints 10 times the numbers, from 0 to 14,
 * @size: is parameter
 * Return: Always 0.
 */
void print_square(int size)
{
int x, y;
if (size <= 0)
_putchar('\n');
else
{
for (x = 0; x < sixe; x++)
{
for (y = 0; y < size; y++)
{
_putchar(35);
}
_putchar('\n');
}
}
}
