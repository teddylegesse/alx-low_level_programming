#include "main.h"
/**
 * _islower - checks if lowercase latter  in the string 'c' isparameter
 * @c: is parameter
 * Return: 1 if c is lowercase
 */
int _islower(int c)
{
return (c >= 'a' && c <= 'z');
}
